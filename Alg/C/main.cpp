
#include <iostream>
#include <math.h>
#include <vector>
#include <cmath>
#include <algorithm> // sort関数を使用するために必要
#include <ctype.h>
using namespace std;
#define repo(i,n) for(int i = 0; i<(int)n; i++);


int main()
{
  int N,Y;
  cin >> N >> Y;
  vector<string> S(H);
  for(auto& row : S) {
    cin >> row;
  }
  
}